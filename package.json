{
  "name": "mon-saas",
  "version": "1.0.0",
  "scripts": {
    "sonar": "sonar-scanner \
      -Dsonar.projectKey=mon-saas \
      -Dsonar.sources=./src \
      -Dsonar.host.url=http://sonarqube:9000 \
      -Dsonar.login=${SONAR_TOKEN}"
  }
}
